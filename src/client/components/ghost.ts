/** A ghost button is drawn in base-content, invisible on a brand surface; this
 * one inherits the surface's own foreground. */
export const GHOST = 'btn btn-ghost text-inherit hover:bg-current/15';
